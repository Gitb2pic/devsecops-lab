# Copie ce fichier vers .env  (cp .env.example .env)  puis change les valeurs.
# .env est ignoré par Git : tes secrets ne partent JAMAIS sur GitHub.
POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=change-moi-mot-de-passe-fort
