# Grafana ajoute automatiquement Prometheus comme source de données au démarrage.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090     # résolu sur le réseau monitoring
    isDefault: true
